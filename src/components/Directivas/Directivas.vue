<script setup>
</script>

<template>

    <!-- Las directivas en vue.js son atributos especiales dentro del DOM, lo que proporciona funcionalidades adicionales al html -->

    <!-- 
    
    v-bind: Enlaza atributos o propidades del DOM con datos del componente 
    Ejemplo: <img v-bind:src="imageURL"/>

    v-model: permite la vinculación bidireccional de datos con elementos de formulario
    Ejemplo: <input v-model="mensaje"/> enlaza el valor del campo de entrada con la propiedad 'mensaje' en el componente, permitiendo la sincronización automatica

    v-for: Renderiza una lista de elementos basado en una colección de datos
    Ejemplo: <li v-for="item in items" :key="item.id">{{ item.name }}</li> itera sobre el array items y renderiza una lista para cada uno de los items

    v-if, v-else-if, v-else:
    Renderiza condicionalmente elementos basados en expresiones
    Ejemplo: <p v-if="show"> Texto visible solo si show es verdadero</p>
     
    
    -->

</template>

<style scoped>

</style>